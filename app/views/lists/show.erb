<style>

input[type="submit"]{
    background: none;
    color: inherit;
    border: none;
    padding: 0;
}

.center-wrapper {
    background-color: white;
    border: 1px solid blue;
    margin: 0 auto;
    dispay: inline-block;
    width: 700px;
    padding-bottom: 200px;
}

body {

    background-image: url("https://cdn.remodelista.com/wp-content/uploads/2016/12/alice-waters-kitchen-vignette-copper-pot-herbs-oils-1466x978.jpg");
    background-repeat: no-repeat;
    background-size: cover;
    background-attachment: fixed;


}



</style>

<div class="center-wrapper">
<h1><%= @list.name %></h1>
Note: later, make spoiled list only display the last 30 items or so spoiled no more. 
dont want to turn it into a guiltfest
<br>
<% if @list.name.downcase != "spoiled" %>

<a href="/items/new/<%= @list.name.downcase %>">Add an Item</a>
<a href="/items/new/<%= @list.name.downcase %>/batch">Batch Add Items</a>
<% end %> 
/items/new/:cabinet/batch






<div class="ui celled list">
<% @list.items.each do | item | %>
    <div class="item">

    <div class="content">

    <div class="header">

        <% if item.amount && item.amount != "" %>
        <%= item.amount %> of 
        <% end %>
        <%=  item.name %>
    </div>
    
    <% if item.fresh_for_x_more_days  %>
    <% if item.fresh_for_x_more_days < 0 %>
        <%= -item.fresh_for_x_more_days %> days past due 
        <% elsif item.fresh_for_x_more_days == 0 %>
        Fresh for today
        <% else %>
        Fresh for <%= item.fresh_for_x_more_days %> more days
      <% end %>
      <% end %>
      <br>

      

          

      <div class="right floated content">


  
      <a class="ui button" href="/items/<%= item.id %>/edit">Edit</a>

      <% if @list.name.downcase != "spoiled" %>

    <a class="ui button" href="/recipes/search/<%=  item.name %>" >Search for recipes</a>
    <% end %>

    <div style="display: inline-block">

<form action="/groceries" method="POST">
<input type="hidden" name="source" value="<%= @list.name.downcase %>">
<input type="hidden" name="name" value="<%=  item.name %>">
<span class="ui button">
<span>
<input type="submit" value="Add to grocery"></span>
</span>
</form>
</div>



    <!--
    MOVE to SPOILED list
    we are sending this form to POST '/items/new'
    this is processed specially because it has the attribute moved_from_cabinet_item_id
    we will create a new item, put it in spoiled list
    remove old item, which automatically removes associations
    -->

    

    <% if @list.name.downcase != "spoiled" %>
    <div style="display: inline-block">

    <form action="/items" method="POST">
    <input type="hidden" name="moved_from_cabinet_item_id" value="<%= item.id %>">
    <input type="hidden" name="name" value="<%= item.name %>" >
    <input type="hidden" name="list"  value="spoiled">
    <input type="hidden" name="amount"  value="<%= item.amount %>">
    <span class="ui button">
    <span>
    <input type="submit" value="Mark as Spoiled">
    </span>

    </form>
    </div>
    <% end %>
    
    <div style="display: inline-block">
    <form action="/items/<%= item.id %>/delete" method="POST">
    <input type="hidden" name="_method" value="delete">
    <span class="ui button">
    <span>
    <input type="submit" value="Delete">
    </span>

    </form>    
    </div>

    <!--
    quick add to fridge
    quick add to freezer  
    (fridge <-> freezer only)
    -->

     
    </div>
    </div>
 
    </div>
    <% end %>
    </div>


<a href="items/edit/<%= @list.name.downcase %>/batch">Add/Remove/Edit Multiple Items</a>

</div> <!-- div center-wrapper -- >